{
  "name": "@neurostack/workflow-engine",
  "version": "0.1.0",
  "description": "Agentic workflow orchestration and execution engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier --write src",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@neurostack/shared": "workspace:*",
    "@neurostack/agent-core": "workspace:*",
    "@neurostack/memory-system": "workspace:*",
    "pino": "^8.17.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.0"
  }
}
