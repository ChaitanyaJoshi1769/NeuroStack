{
  "name": "@neurostack/anomaly-feedback",
  "version": "1.0.0",
  "description": "Anomaly detection feedback loop for continuous model improvement",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src",
    "test": "jest"
  },
  "dependencies": {
    "@neurostack/shared": "workspace:*",
    "@neurostack/ml-anomaly-detector": "workspace:*",
    "@neurostack/ml-finetuning": "workspace:*",
    "pino": "^8.14.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
