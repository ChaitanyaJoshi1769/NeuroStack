{
  "name": "@neurostack/model-deployment",
  "version": "1.0.0",
  "description": "Model deployment orchestrator with canary deployments, A/B testing, and rollback capabilities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src",
    "test": "jest"
  },
  "dependencies": {
    "@neurostack/shared": "workspace:*",
    "@neurostack/model-registry": "workspace:*",
    "@neurostack/inference-server": "workspace:*",
    "@neurostack/distributed-logging": "workspace:*",
    "@neurostack/data-quality": "workspace:*",
    "pino": "^8.14.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
