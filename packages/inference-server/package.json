{
  "name": "@neurostack/inference-server",
  "version": "1.0.0",
  "description": "High-performance inference server for ML model serving and predictions",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src",
    "test": "jest"
  },
  "dependencies": {
    "@neurostack/shared": "workspace:*",
    "@neurostack/model-registry": "workspace:*",
    "@neurostack/feature-store": "workspace:*",
    "@neurostack/redis-cache": "workspace:*",
    "pino": "^8.14.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
