{
  "name": "@neurostack/kafka-integration",
  "version": "1.0.0",
  "description": "Kafka integration for distributed event streaming and processing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src",
    "test": "jest"
  },
  "dependencies": {
    "@neurostack/shared": "workspace:*",
    "@neurostack/streaming-core": "workspace:*",
    "@neurostack/metrics-aggregation": "workspace:*",
    "kafkajs": "^2.2.4",
    "pino": "^8.14.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
