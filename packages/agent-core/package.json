{
  "name": "@neurostack/agent-core",
  "version": "0.1.0",
  "description": "Core agent framework for NeuroStack",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier --write src",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@neurostack/shared": "workspace:*",
    "@neurostack/semantic-engine": "workspace:*",
    "@neurostack/vector-runtime": "workspace:*",
    "@neurostack/memory-system": "workspace:*",
    "uuid": "^9.0.1",
    "pino": "^8.17.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.0"
  }
}
