{
  "name": "@neurostack/ontology",
  "version": "0.1.0",
  "description": "Business ontology engine for NeuroStack",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier --write src",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@neurostack/shared": "workspace:*",
    "uuid": "^9.0.1",
    "pino": "^8.17.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.0"
  }
}
